import * as React from 'react'
import { Information } from '../components/'

interface IUpplysingarProps {}

const Upplysingar: React.FunctionComponent<IUpplysingarProps> = (props) => {
  return <Information />
}

export default Upplysingar
